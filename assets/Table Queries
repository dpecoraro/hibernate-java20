CREATE TABLE Books (
    Id               INTEGER PRIMARY KEY
                             UNIQUE
                             NOT NULL,
    Title            TEXT    NOT NULL,
    Ammount_of_Pages INTEGER NOT NULL,
    Genres           TEXT
);

CREATE TABLE Authors (
    Id   INTEGER PRIMARY KEY
                 UNIQUE
                 NOT NULL,
    Name TEXT    NOT NULL
);

